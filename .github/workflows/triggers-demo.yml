name: Trigger Demo

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  demo:
    runs-on: ubuntu-latest
    steps:
      - name: Print event name
        run: echo "Workflow triggered by: ${{ github.event_name }}"
